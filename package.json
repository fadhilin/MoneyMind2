{
  "name": "financecontrol",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
"scripts": {
  "dev": "cd apps/web && npx vite"
},
  "overrides": {
    "esbuild": "0.25.0"
  }
}